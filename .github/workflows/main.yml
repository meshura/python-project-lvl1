name: main
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: |
          make install
      - name: Run linter
        run: |
          make lint